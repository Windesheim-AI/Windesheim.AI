{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "jsx": "react",
    "types": ["jest"],
    "noEmit": true,
  },
  // only compile the production TS files
  "exclude": ["__tests__/*"]
}