{
    "extends": "expo/tsconfig.base",
    "compilerOptions": {
        "strict": true,
        "jsx": "react",
        "types": ["jest", "node"],
        "noEmit": true
    },
    // only compile the production TS files
    "exclude": ["__tests__/*", "cypress/*"],
    "include": ["cypress/**/*.ts"]
}
